# Notification-Reminder-With-Sound-Using-Python

### Libraries Used:
##### plyer
##### pyttsx3
A simple desktop notifier application using Python, which produces a notification message and also reads the same notification message for the user.
